name: parent workflow

on:
  workflow_calls:
    inputs:
      data:
        required: false
        type: string
        default: "DEV"

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ data }} event."
    
